//- child component
.ladder
  each stage, index in ladder
    if (index < currentPosition)
      if (stage.isFixed)
        .done-immune= stage.prize
      else
        .done= stage.prize
    else
      if (stage.immune)
        .immune= stage.prize
      else
        .general= stage.prize

