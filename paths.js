const build = './build';
const src = './src';

const paths = {
    src,
    build,
    html_src: src + '/html',
    sass_src: src + '/sass',
    js_src: src + '/js',
    res_src: './resources',
    html_build: build,
    css_build: build,
    js_build: build + '/js',
    res_build: build,
}

module.exports = paths;